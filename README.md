xcats is a javascript app to register the mutant cats of your neighbourhood. List their names, date of birth and mutant power.

git add filename
git commit -m "Commit message"
git push -u origin master